# Create CustomTabBar Component

## 1. Create Component File
*   **File**: `src/components/CustomTabBar.vue`
*   **Content**: Implement the provided code template.
    *   **Structure**: Fixed bottom container with gradient background.
    *   **Logic**:
        *   `tabList`: Array of tab items (Main Hall, Workshop, Theater, Wiki, Market).
        *   `switchTab`: Use `uni.reLaunch` to navigate, solving the stack issue mentioned by the user.
        *   `currentPath` prop: To highlight the active tab.
    *   **Style**: SCSS for fixed positioning, safe area, and active states.

## 2. Integration (Mental Check)
*   The user only asked to *create* the component in this turn.
*   I will strictly follow the instruction to create `src/components/CustomTabBar.vue`.
*   I will not modify other pages yet unless implicitly required, but the prompt says "1. Create Component... Create file...". It implies just creation. I will stick to creation.

